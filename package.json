{
  "name": "dana-webhook",
  "version": "1.0.0",
  "description": "Simple Express server with 2 public POST endpoints that save incoming data as .txt (JSON format)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
